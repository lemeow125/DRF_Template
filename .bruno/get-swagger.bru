meta {
  name: GET Swagger
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/swagger/
  body: none
  auth: none
}

tests {
  test("should return 200 HTTP Response Code", () => {
    expect(res.status).to.equal(200);
  });
}
