meta {
  name: GET Swagger
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/swagger/
  body: none
  auth: none
}

tests {
  test("should return the swagger page", function () {
    expect(res.getStatus()).to.equal(200);
  });
}