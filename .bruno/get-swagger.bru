meta {
  name: GET Swagger
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/swagger/
  body: none
  auth: none
}

tests {
  try {
    expect(res.status).to.equal(200);
  } catch (e) {
    console.log("Trace:", res.body);
    throw e;
  }
}
