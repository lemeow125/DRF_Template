meta {
  name: GET Swagger
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/swagger/
  body: none
  auth: none
}

tests {
  test("Should return the Swagger page", () => {
    if (res.status === 200) {
      // pass
    } else {
      throw new Error(res.body);
    }
  });
}